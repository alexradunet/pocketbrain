FROM oven/bun:1.3.9-alpine

RUN apk add --no-cache git bash docker-cli docker-cli-compose curl

RUN BUN_INSTALL=/usr/local bun add -g opencode-ai@1.1.53

WORKDIR /workspace/pocketbrain

CMD ["sleep", "infinity"]
