# Required
OPENCODE_API_KEY=your-opencode-api-key-here

# Tailscale (get auth key from https://login.tailscale.com/admin/settings/keys)
# Use a REUSABLE auth key — single-use keys will fail on container restart.
# After first connect, you can remove TS_AUTHKEY; existing state is reused.
TS_AUTHKEY=your-tailscale-auth-key-here
TS_HOSTNAME=pocketbrain

# Optional
ASSISTANT_NAME=Andy
TZ=America/New_York

